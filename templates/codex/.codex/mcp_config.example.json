{
    "$schema": "https://raw.githubusercontent.com/modelcontextprotocol/spec/main/schema/mcp.schema.json",
    "mcpServers": {
        "filesystem": {
            "command": "npx",
            "args": [
                "-y",
                "@modelcontextprotocol/server-filesystem",
                "."
            ]
        }
    },
    "notes": {
        "usage": "Adapt this example to your Codex runtime if MCP is enabled.",
        "security": "Use least-privilege server configuration."
    }
}