{
    "hooks": {
        "PreToolUse": [],
        "PostToolUse": [
            {
                "name": "Run deploy check manually after major changes",
                "command": "powershell -ExecutionPolicy Bypass -File .\\deploy.ps1 -Check"
            }
        ]
    },
    "notes": {
        "usage": "Merge entries into .claude/settings.local.json as needed."
    }
}