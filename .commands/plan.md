# Command: p/ Plan Gate

When the user includes `p/`:

Output behavior:
- define scope and constraints first
- prefer `skills/writing-plans` for multi-step work
- do not start implementation until plan is coherent

Response pattern:
- signal
- goal and constraints
- phased plan
- first executable step
