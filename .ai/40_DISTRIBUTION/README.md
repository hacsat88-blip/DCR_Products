# 40 DISTRIBUTION

Purpose:
Explain how canonical sources become model-specific entrypoints and mirrors.

Read:

- `.ai/distribution`
- `tools/adapters`
- `templates`
- `deploy.ps1`
- `tools/deploy-all.ps1`

Generated targets include `AGENTS.md`, `CLAUDE.md`, `.github/copilot-instructions.md`,
`.codex/agents`, `.claude/agents`, and `.cursor/rules`.
Fix the source, manifest, template, or adapter instead of editing generated output.
