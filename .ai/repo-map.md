# Repo Map

Purpose:
Help the assistant understand the project structure and conventions
before proposing changes.

Behavior rules:

- infer the repository structure before suggesting edits
- prefer existing patterns instead of introducing new ones
- preserve architecture and naming conventions
- recommend minimal changes in the correct layer
- avoid cross-layer coupling unless redesign is requested

When uncertain:

- point to the most likely file or layer that should be inspected
- state assumptions explicitly

---

## Project Summary

Product:
DCR Products — AI prompt and agent configuration management system.

Main purpose:
Manage and deploy AI system prompts, agent rules, and operational configurations
across multiple editors and CLIs (VS Code Copilot, Copilot CLI, Claude Code,
Codex, Cursor).

Tech stack:
Markdown-based configuration, GitHub for version control, PowerShell deploy.

Runtime:
VS Code / GitHub Copilot Chat / GitHub Copilot CLI

Package manager:
N/A (configuration repository)

---

## Entrypoints

Each editor/CLI reads its own entry point from the workspace root.
All entry points share the same DCR Kernel philosophy but are formatted
for each tool's reading conventions.

| Editor/CLI      | Entry Point                         | Reads                                                            |
| --------------- | ----------------------------------- | ---------------------------------------------------------------- |
| VS Code Copilot | .github/copilot-instructions.md     | .ai/, .ai/kernel/gates/, .ai/assets/rules/, .ai/assets/skills/ |
| Copilot CLI     | AGENTS.md                           | .ai/repo-map.md, .ai/assets/rules/, .ai/assets/skills/         |
| Codex           | AGENTS.md                           | .ai/assets/rules/ (by reference)                                |
| Claude Code     | CLAUDE.md                           | .ai/assets/rules/ (by reference)                                |

---

## Main Directories

Repository governance model:

- Navigation layer: numbered AI map (`.ai/00_START_HERE.md`, `.ai/10_CONTROL/`, `.ai/20_SOURCE/`, `.ai/30_ROUTING/`, `.ai/40_DISTRIBUTION/`, `.ai/50_EXTERNAL/`, `.ai/90_COMPAT/`)
- Source layer: editable canonical assets (`.ai/kernel/`, `.ai/assets/rules/`, `.ai/assets/skills/`, `templates/`, `.ai/assets/agents/`)
- Runtime layer: tool entrypoints read directly by each editor (`.github/`, `AGENTS.md`, `CLAUDE.md`)
- Generated layer: deploy outputs and mirrors (`.claude/agents/`, `.codex/agents/`; large mirrors are Git-ignored)
- Workspace layer: editor settings, docs, and operations scripts (`.vscode/`, `docs/`, `deploy.ps1`, `validate.ps1`, `init-project.ps1`)

Unsafe migration rule:

- Do not move `.ai/assets/rules/` or `.ai/assets/skills/` into runtime/generated mirrors
- Do not delete `templates/` while `init-project.ps1` reads from it
- Do not overwrite runtime entrypoints from templates without an explicit migration plan
- Do not manually edit `AGENTS.md`, `CLAUDE.md`, or `.github/copilot-instructions.md` — these are regenerated by `deploy.ps1`
- Any future path migration must support old and new paths in parallel before deletion

## Execution Layer

.github/copilot-instructions.md
VS Code Copilot entry point. DCR Kernel with Permission model (P1/P2/P3)
and routing priority.

AGENTS.md
Codex / GitHub Copilot CLI entry point. DCR Kernel + module behaviors.
Copilot CLI specific behavior is documented in `.ai/environments/copilot-cli/kernel.md`.

CLAUDE.md
Claude Code entry point. DCR Kernel + safety boundaries + permission model.

.ai/
AI system kernel, repo map, routing design, and behavior modules.

.ai/00_START_HERE.md
First-read navigation entrypoint for agents. It explains the numbered layer order
and points to the current source-of-truth registries without moving files.

.ai/10_CONTROL/
Navigation layer for control-plane metadata. The concrete control-plane files
remain in `.ai/control-plane/`.

.ai/20_SOURCE/
Navigation layer for canonical source assets. Rules now point to `.ai/assets/rules`;
remaining source groups still point to `.ai/assets/`, `.ai/assets/books/`, `.ai/kernel/`,
`.ai/core/modules/`, and `.ai/environments/`.
The machine-readable source layout map is `.ai/20_SOURCE/source-layout.json`.

.ai/30_ROUTING/
Navigation layer for routing, gates, and local routing state boundaries.

.ai/40_DISTRIBUTION/
Navigation layer for manifests, adapters, templates, deploy targets, and mirrors.

.ai/50_EXTERNAL/
Navigation layer for external capability intake and home inventory classification.

.ai/90_COMPAT/
Navigation layer for legacy path maps and staged migration compatibility.

.ai/kernel/
共通カーネル正本。全エディタの入口ファイルはこの思想を継承。

.ai/repo-map.md
このファイル。プロジェクト構造と規則の参照用。

.ai/rule-routing-design.md
ルール動的参照の設計文書。単一ソース原則、auto-load 安全条件、
エディタ別マッピングを定義。

.ai/core/modules/
Specialized behavior modules (architecture, debugging, review, prompting).

.ai/kernel/gates/
Trigger-activated gate definitions (a/, i/, r/, s/, d/, p/, q/, sh/).
旧 `.commands/` は `.ai/kernel/gates/` に統合済み。

.ai/kernel/dcr-kernel.md
Shared runtime kernel source. Runtime entrypoints are generated from this file.

.vscode/
VS Code workspace settings (useInstructionFiles: true).
Workspace config layer。`.ai/assets/rules/` や `templates/` の代替ではない。

## Asset Layer

.ai/assets/rules/
Agent-specific behavior rules (62 agents). 正本。
`_` プレフィクスファイルは deploy 対象外の参照ドキュメント:

- _METADATA.md: メタデータポリシー定義
- _coding-standards.md: コードスタイル & セキュリティベースライン (ECC 由来)
- _testing-standards.md: テスト基準 & TDD 原則 (ECC 由来)
- _git-conventions.md: コミット & ブランチ規約 (ECC 由来)

.ai/assets/skills/
Skill definitions (70 skills). Git-managed source of truth.
deploy.ps1 で ~/.agents/skills/ (VS Code Copilot) へ配布。

ECC 部分採用スキル (3):

- search-first/: 実装前調査ワークフロー
- strategic-compact/: 論理的コンテキスト整理
- tdd-workflow/: テスト駆動開発サイクル

templates/
Model-specific configuration templates for init-project.ps1.
Subdirs: claude-code/, codex/, vscode-copilot/
Source layer。runtime ではなく project-init 用の入力契約。

templates/README.md
テンプレートの配置ルールと使い方。

templates/project-context.md
プロジェクト固有情報のシングルソース定義。各テンプレートの {placeholder} に対応する
key: value を記述する。新規プロジェクト作成時にコピーして編集する。

prototypes/
Experimental projects and proof-of-concepts.

## Deploy

deploy.ps1
一方向同期スクリプト。サトシ開発 → 各エディタのユーザーレベルパスへ配布。

- .ai/assets/skills/ → ~/.agents/skills/ (VS Code Copilot)
- .ai/assets/agents/*.toml → .codex/agents/ (Codex, Git 管理外)
- .ai/assets/agents/*.md → .claude/agents/ (Claude Code, Git 管理外)

オプション: -Target (all/vscode/cursor/agents/dcr), -DryRun, -Check (drift検出)。

sync-agents.ps1
レガシー: deploy.ps1 -Target agents に統合済み。互換性維持のため残存。

## Project Init

init-project.ps1
新規プロジェクト向け AI 指示ファイル生成スクリプト。
templates/ のテンプレートと project-context.md を入力に、
CLAUDE.md / AGENTS.md / .github/copilot-instructions.md の3ファイルを生成。
共有リソース (.ai/kernel/, .ai/core/modules/) もコピーする。
オプション: -ProjectPath (必須), -ContextFile, -Target (all/claude/codex/copilot), -SkipShared, -DryRun。

---

## Architecture Notes

Preferred layering:
copilot-instructions.md → kernel.md → module/*.md + .ai/kernel/gates/*.md

Configuration pattern:
Markdown-based instruction files, no code dependencies.

Deployment pattern:
deploy.ps1 syncs `.ai/assets/skills/`, `.ai/assets/rules/`, and `.ai/assets/agents/` to editor-specific paths.
One-way only: repository → editor. Never reverse.

---

## Change Rules

When modifying this repository:

- edit `.ai/assets/rules/`, `.ai/assets/skills/`, `templates/`, and `.ai/assets/agents/` here, then deploy if needed
- prefer minimal diffs
- preserve existing naming conventions
- do not create files in templates/ unless archiving a new editor config or managing project init templates
- do not modify ~/.agents/ directly
- do not treat `.claude/agents/` or `.codex/agents/` as editable source paths
- do not perform one-shot move + overwrite + delete migrations for folder restructuring
