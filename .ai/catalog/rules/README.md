# Rules Moved

Rule source files moved to `.ai/assets/rules`.

This directory is retained as a compatibility marker. Do not add active rule
files here. Use `.ai/assets/rules` as the current primary source path.
