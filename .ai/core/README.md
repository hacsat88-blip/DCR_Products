# AI Core

Future physical source layout for kernel, gates, permissions, quality policy, and shared behavior modules.

Current stage: mapped only. Existing content remains in `.ai/kernel`, `.ai/core/modules`, and `.ai/environments` until a verified migration switches the primary paths.
