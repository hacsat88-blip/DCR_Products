# Rules

Future home for rule source files.

Current primary: `.ai/assets/rules`.

This directory is authoritative for rule source files. The legacy
`.ai/catalog/rules` directory is retained as a compatibility marker only.
