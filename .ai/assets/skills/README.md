# Skills

Canonical home for skill source directories.

Current primary: `.ai/assets/skills`.

Each skill lives in its own directory with a `SKILL.md` entrypoint. Keep
path-sensitive scripts using `Resolve-DcrSourcePath` or
`Get-DcrResolvedSourceRelativePath` for `AssetType "skills"`.
