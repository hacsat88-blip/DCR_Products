# Routing

Control-plane routing metadata.

- `state.schema.json`: schema for local routing state.
- `state/`: local generated runtime state. This directory is ignored and should not be committed.
- `router/`: future router definitions.
- `fixtures/`: future routing fixtures.
