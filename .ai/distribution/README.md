# AI Distribution

Distribution metadata for generated entrypoints and tool-specific mirrors.

- `manifests/`: model/editor target declarations.
- `adapters/`: future adapter source location.
- `templates/`: future template source location.

Current executable adapters remain in `tools/adapters/`.
