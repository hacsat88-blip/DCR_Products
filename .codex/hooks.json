{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash(git commit *)",
        "hooks": [
          {
            "type": "command",
            "command": "powershell -ExecutionPolicy Bypass -File .\\validate.ps1; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -ExecutionPolicy Bypass -File .\\deploy.ps1 -Check; exit $LASTEXITCODE",
            "statusMessage": "Hook gate: running validate and deploy check before git commit"
          }
        ]
      },
      {
        "matcher": "Bash(git push *)",
        "hooks": [
          {
            "type": "command",
            "command": "powershell -ExecutionPolicy Bypass -File .\\validate.ps1; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -ExecutionPolicy Bypass -File .\\deploy.ps1 -Check; exit $LASTEXITCODE",
            "statusMessage": "Hook gate: running validate and deploy check before git push"
          }
        ]
      }
    ]
  }
}
